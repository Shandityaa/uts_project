name: uts_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # STATE MANAGEMENT (WAJIB UTS)
  provider: ^6.1.2

  # API NETWORKING (WAJIB UTS)
  dio: ^5.4.0

  # FIREBASE (WAJIB UTS)
  firebase_core: ^3.4.0
  firebase_auth: ^5.1.3
  google_sign_in: ^6.2.1
  firebase_messaging: ^15.0.3

  # ONBOARDING MUNCUL SEKALI SAJA
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
